# stockManager
Proyecto Generacion T

Previamente antes a iniciar el backend, se debe tener en MySql una base de datos creada, con el nombre "desktopHosco"

Tambien tener el archivo ".env" con estos datos:
hostDb, nameDb, passwDb, secretWord, port y userDb

Para levantar el backend, son estos pasos:
Abrir una nueva consola en la carpeta "api"
Instalar dependencias con "npm i"
Levantar el servicio con "npm start"

Para levantar el frontend, son estos pasos:
Abrir una nueva conosla en la carpeta "desktop"
Instalar dependencias con "npm i"
Levanta el servicio con "npm run dev"
La url que salta en la terminal, es la dirección, el sistema inicia en la ruta "/login"

Usuarios default del sistema:
Username: admin Password: admin Rol: admin
Username: team Password: team Rol: team